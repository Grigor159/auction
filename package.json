{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n NEST,NEXT -c blue,green \"npm run start:dev --prefix backend\" \"npm run dev --prefix web\"",
    "build": "npm run build --prefix back && npm run build --prefix front",
    "install:all": "npm install --prefix back && npm install --prefix front"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}